services:
  paperless-rest:
    build:
      context: ./PaperlessREST
      dockerfile: Dockerfile
    ports:
      - 8081:8081

  paperless-ui:
    build:
      context: ./PaperlessNgx
      dockerfile: Dockerfile
    ports:
      - 80:80
